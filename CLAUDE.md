# 日语影子跟读 PWA

个人用日语影子跟读 app。每天一课，素材来自日文 AI 媒体，改写成会议口语后合成音频。
用户是中文母语，在日本人项目组工作，目标是开会时能听懂并能表达自己的想法。

## 部署
GitHub Pages。push 到 main 即自动部署。手机上「添加到主屏幕」当 app 用。

## 改动指南（手机上改代码时看这里）

**前端在这三个文件，随便改：**
- `index.html` — 结构
- `app.css` — 样式（主题变量在 `:root`，暗色是默认，亮色在 media query 里）
- `app.js` — 逻辑

**改前端不需要碰 `tools/` 下的流水线。**

### 录音跟读模式

播放器的 🎙 开关。每句读完 → 自动暂停 → 提示音（升调）→ 录你读 → 静音 750ms 自动收尾
（降调提示音）→ 打分 → 自动进下一句。全程可以不看屏幕。

打分三项：
- **認識率** — Web Speech API(ja-JP) 转写你说的，跟原文做字符 bigram 相似度。
  比对时用 `ja` 和 `asr` 两个参照取高分：`asr` 是「音声認識が書きそうな表記」
  （RIZAP→ライザップ），专门消除表记差异造成的白扣。
- **速度** — 你实际说话时长 vs 原音时长。用 AnalyserNode 做音声区間検出量出来的，
  不是录音窗口长度（那是固定的，量不出速度）。
- **A/B 回放** — ▶自分 / ▶お手本。

认识率是**相对指标**，用来看同一句在不同天有没有进步，不是绝对发音分。
浏览器里做不出真正的发音评测（那要专门的语音评测 API）。

### 三条不要破坏的约束

1. **`sw.js` 的缓存策略不要动成全局 cache-first。**
   现在是：app 外壳（html/css/js）网络优先，教材（`content/日期/`）缓存优先。
   改成全局缓存优先的话，以后 push 了代码手机上也看不到更新。

2. **中文默认隐藏。** 看得见翻译就不会去听日语。`body.showzh` 控制，默认关。

3. **锁屏控制映射到「上一句/下一句」**（`media()` 里的 `previoustrack`/`nexttrack`）。
   用户主要在走路时用，靠耳机键重放没跟上的那句。不要改成切换课程。

### 教材难度标准（改 `tools/build.mjs` 的 prompt 时看这里）

用户是中文母语，**汉字熟语（推論・精度・検証）对他是白送的，不算生词**。
难度必须压在这四类上：和語動詞・複合動詞 / 商务惯用语 / 片假名职场语 / 文末留白表达。
语速：慢速 `+15%`、常速 `+35%`（`tools/tts.py`）。这是跟用户实测校准过的，别随便改。

### 改了已发布日期的教材内容怎么办

`content/<日期>/` 是缓存优先（教材按天不变，离线优先）。改了已存在日期的 `lesson.json`
或 mp3，手机上不会更新——要**把 `sw.js` 里的 `V` 版本号 +1** 才会重新拉。

## 数据结构
`content/index.json` 是日课索引；`content/<日期>/lesson.json` 含稿子、生词、句型、
以及每句音频的精确起止 `audio.<track>.spans`（逐句合成拼接算出来的，不是估的）。

## 本地跑
```bash
python3 -m http.server 8899        # 然后开 localhost:8899
tools/daily.sh                     # 生成今天的一课（周末跳过，FORCE=1 强制）
tools/daily.sh 2026-09-10          # 指定日期
```
